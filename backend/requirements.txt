# Floors are deliberately loose where spotdl pins its own versions.
fastapi>=0.115
uvicorn[standard]>=0.32
aiosqlite>=0.20
pydantic>=2.9
pydantic-settings>=2.6
mutagen>=1.47
httpx>=0.27
Pillow>=10
spotipy>=2.24
ytmusicapi>=1.8
# NOTE: spotdl is NOT installed — v4.5.2 pins fastapi<0.104 which conflicts.
# The matcher uses its own ISRC + duration/title verification gate (ytmusicapi);
# app/engine/matcher.py imports spotdl defensively and skips it when absent.
# yt-dlp floor for image build; entrypoint upgrades to nightly (--pre) at runtime.
yt-dlp[default]>=2026.7.4
bgutil-ytdlp-pot-provider>=1.0
